# REVERT:

# To Undo the latest commit operation with the commit message:

               git revert (last commit id)

# RESET:

# It used to undo multiple commits:
# git reset --soft ( only delete commits id)
# git reset --hard (delete with contents and commits id)

               git reset (commit id to be last commit) 
